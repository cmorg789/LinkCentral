# Server Configuration
HOST=0.0.0.0
PORT=8000
SOAP_PATH=/ScriptLinkService.asmx

# Database (request logging)
DATABASE_URL=sqlite:///data/scriptlink.db

# Security - Only required if using password_encrypted in connections.yaml (recommended)
# Generate a key (minimum 32 characters): python -c "import secrets; print(secrets.token_urlsafe(32))"
# SECRET_KEY=

# Script Execution
# Maximum script execution time in seconds
SCRIPT_TIMEOUT=30
# True = unhandled errors block form, False = show as alert
SCRIPT_ERROR_BLOCKING=False

# How often to prune old request logs (minutes) (0 = disabled)
CLEANUP_INTERVAL_MINUTES=0
# Delete logs older than this many days
CLEANUP_RETENTION_DAYS=30

# Debugging - Enable verbose logging
DEBUG=False
